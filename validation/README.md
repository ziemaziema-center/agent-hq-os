# Validation

Validate that every task has a boot record, at least one validation step, and post-task telemetry. A future script should fail if a patch lacks memory updates.

## Repo-specific Demo Validation

Run:

```bash
python setup_demo_workspace.py
```

This is the repo-specific fixture path. It is intentionally local-only.

