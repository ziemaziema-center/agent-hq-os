# Starter Kit

Copy `project_bootstrap/` into a project to create a minimal Agent HQ OS install. The kit is intentionally plain Markdown so it works with ChatGPT, Claude, Claude Code, Codex, Cursor, and local workflows.

