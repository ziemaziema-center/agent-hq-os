# OpenRouter Usage Example

Use OpenRouter for model access while keeping keys outside the repo. Agent HQ OS does not care which model provider is used; it cares that the session boots from memory and validates before closeout.

