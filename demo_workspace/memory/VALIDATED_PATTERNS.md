# Validated Patterns

## Pattern: Fixture Before Integration

Use local fixture files before connecting any external API, publisher, bot, exchange, or workflow engine.

Validation: local demo task writes expected telemetry without network access.

